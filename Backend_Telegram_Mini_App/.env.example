# Copy to .env for local testing (the worker reads these variables).
MINI_BACKEND_BASE_URL=http://localhost:8000
# Must exactly match BOT_SERVICE_KEY on the main API.
MINI_BOT_SERVICE_KEY=replace-with-the-api-worker-secret
LOOP_SECONDS=0.5
